# The only application env var: base URL of the news engine (Hacker News API).
# Read server-side at request time, so one built image works in every environment.
NEWS_API_URL=https://hacker-news.firebaseio.com/v0
